FROM itzg/minecraft-bedrock-server:latest

RUN apt update && apt install -y screen --no-install-recommends \
    && apt clean \
    && rm -rf /var/lib/apt/lists/*

RUN chown -R 10001:10001 /data \ 
    && chmod 755 /data

USER 10001

WORKDIR /data